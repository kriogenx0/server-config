# OS / editor cruft
.DS_Store
*.swp

# Logs
*.log
logs/

# Build/output directories
dist/

# This repo tracks only the shared nginx snippets. Stock nginx files and
# per-app site configs live on the server / in each app's own repo.
nginx/*
!nginx/snippets/
